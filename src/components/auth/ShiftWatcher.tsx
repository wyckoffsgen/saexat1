// Shift system removed — fingerprint shift verification was scrapped per product request.
// This component is kept as a no-op so existing imports continue to work.
export function ShiftWatcher() {
  return null;
}
